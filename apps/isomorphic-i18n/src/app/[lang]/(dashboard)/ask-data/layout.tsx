import { metaObject } from "@/config/site.config";

export const metadata = metaObject("Ask Data");

export default function AskDataLayout({ children }: { children: React.ReactNode }) {
  return children;
}
